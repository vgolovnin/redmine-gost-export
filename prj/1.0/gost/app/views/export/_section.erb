<%
  files = []
  section.attachments.each { |attachment|
      files << {filename: attachment.filename, title: attachment.description}
      FileUtils.cp attachment.diskfile, File.join(pwd, attachment.filename)
}
%>
<%= raw latex(section.text, files: files) %>
